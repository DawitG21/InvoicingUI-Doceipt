import { MockTopSecretService } from './mock-top-secret-service';

describe('MockTopSecretService', () => {
  it('should create an instance', () => {
    expect(new MockTopSecretService()).toBeTruthy();
  });
});
